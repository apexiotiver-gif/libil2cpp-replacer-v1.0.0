jdk:
  - openjdk11
before_install:
  - mkdir -p gradle/wrapper
  - wget -q https://raw.githubusercontent.com/gradle/gradle/v6.9.0/gradle/wrapper/gradle-wrapper.jar -O gradle/wrapper/gradle-wrapper.jar
  - chmod +x gradlew
